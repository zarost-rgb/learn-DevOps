---
- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install essential system packages
  apt:
    name:
      - curl
      - git
      - unzip
    state: present
